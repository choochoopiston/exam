<a href="#" class="dropdown-toggle" data-toggle="dropdown">
    
    <i class="fa fa-envelope"></i> <span id="unreadmessage"><%= @notifications_messages_count %></span>
    
</a>
<ul class="dropdown-menu">
    <%= render partial: 'notifications/message', locals: { notifications_messages: @notifications_messages } %>
    <li><%= link_to "全てを見る", message_user_notifications_path(current_user) %></li>
</ul>