<a href="#" class="dropdown-toggle" data-toggle="dropdown">
    
    <i class="fa fa-user-plus"></i> <span id="unreadfollower"><%= @notifications_followers_count %></span> <span class="caret"></span>
    
</a>
<ul class="dropdown-menu">
    <%= render partial: 'notifications/follower', locals: { notifications_followers: @notifications_followers } %>
    <li><%= link_to "全てを見る", follower_user_notifications_path(current_user) %></li>
</ul>