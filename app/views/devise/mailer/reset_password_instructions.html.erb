<p>こんにちは <%= @resource.email %>さん!</p>

<p>以下のリンクより、パスワードを変更することが可能です。</p>

<p><%= link_to 'パスワードを変更する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>もしパスワード変更手続きをしていない場合は、こちらのメールは削除してください。.</p>
<p>上記のリンクにアクセスしなければ、パスワードが変更されることはありません。</p>

